# ─── Core Framework ───────────────────────────────────────────────────────────
fastapi
# Use standard uvicorn without the [standard] extras to avoid heavy optional deps
uvicorn
pydantic
python-dotenv
python-multipart

# ─── External APIs ────────────────────────────────────────────────────────────
groq
requests

# ─── Graph Database ───────────────────────────────────────────────────────────
neo4j

# ─── Streaming (Sprint 2) ─────────────────────────────────────────────────────
# kafka-python is ~5 MB installed — safe for the Vercel 225 MB bundle limit.
# Used for the real-time /api/fda-alerts/stream SSE endpoint.
kafka-python

# ─── EXCLUDED — exceeds Vercel 225 MB bundle limit ───────────────────────────
# snowflake-connector-python  (~50 MB) → deploy as separate Railway microservice
# numpy                       (~35-70 MB) → not used in API layer
# pandas                      (~60-95 MB) → not used in API layer

